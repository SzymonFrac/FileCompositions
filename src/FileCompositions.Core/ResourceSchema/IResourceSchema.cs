namespace FileCompositions.Core.ResourceSchema;

internal interface IResourceSchema
{
    // Would be qualitied..
    // But it can't know what the qualities based on key...?
    // The schema could not get the resources...
    //IDirectoryDefinition? GetDirectoryDefinition(DirectoryDefinitionKey key);
}
